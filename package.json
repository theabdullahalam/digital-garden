{
  "name": "garden-one",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch:sass": "sass --watch src/scss:src/css",
    "build:sass": "sass src/scss:src/css",
    "watch:eleventy": "eleventy --serve --incremental",
    "build:eleventy": "eleventy --incremental",
    "start": "build:sass; run-p watch:*",
    "build": "run-s build:*"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@11ty/eleventy": "^1.0.1",
    "node-sass": "^7.0.1",
    "npm-run-all": "^4.1.5"
  }
}
